# RUNBOOK_MEMORY_OS

The Memory OS maintenance and Qdrant reconciliation procedure has moved to
[`skills/memory/SKILL.md`](skills/memory/SKILL.md).

Use that skill for SQLite source-of-truth checks, online backup requirements,
Qdrant dry runs and rebuilds, stale-point limitations, SQLite-only fallback,
verification, and rollback. This compatibility file intentionally contains no
operational commands so the procedure has one maintained entry point.
